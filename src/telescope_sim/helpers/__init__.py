"""Bundled aperture and pupil helpers.

This subpackage hosts small helper modules that ``external_pupil`` aperture
configurations can reference (e.g. ``telescope_sim.helpers.miles_pupil``).
Helpers are intentionally lightweight and self-contained so the package can
ship them without depending on external pupil-construction code.
"""
